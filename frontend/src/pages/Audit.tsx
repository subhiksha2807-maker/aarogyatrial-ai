import { FileClock } from 'lucide-react'
import { useEffect, useState } from 'react'
import { api, formatDateTime } from '../api'
import { ErrorBox, Loading, PageHeader } from '../components'
import type { AuditEntry } from '../types'
export default function Audit() { const [items, setItems] = useState<AuditEntry[] | null>(null); const [error, setError] = useState(''); useEffect(() => { api<AuditEntry[]>('/audit').then(setItems).catch(e => setError(e.message)) }, []); if (error) return <ErrorBox message={error} />; if (!items) return <Loading />; return <><PageHeader eyebrow="Accountability" title="Audit history"><p>A readable record of event creation and PI decisions. This demo does not claim cryptographic audit protection.</p></PageHeader><section className="card overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-4">Time</th><th className="px-5 py-4">Event</th><th className="px-5 py-4">Actor</th><th className="px-5 py-4">Action</th><th className="px-5 py-4">Detail</th></tr></thead><tbody className="divide-y divide-slate-100">{items.map(item => <tr key={item.id} className="align-top"><td className="whitespace-nowrap px-5 py-4 text-xs text-slate-500">{formatDateTime(item.created_at)}</td><td className="px-5 py-4"><p className="font-mono font-semibold">{item.participant_code}</p><p className="text-xs text-slate-500">#{item.adverse_event_id} · {item.study_code}</p></td><td className="px-5 py-4"><p className="font-medium">{item.actor_name}</p><p className="text-xs capitalize text-slate-500">{item.actor_role.replace('_', ' ')}</p></td><td className="px-5 py-4"><span className="inline-flex items-center gap-1.5 rounded-full bg-teal-50 px-2.5 py-1 text-xs font-bold text-teal-800"><FileClock size={12} />{item.action}</span></td><td className="max-w-md px-5 py-4 text-sm leading-relaxed text-slate-600">{item.detail}</td></tr>)}</tbody></table></div></section></> }
